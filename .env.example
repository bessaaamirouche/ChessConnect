# =============================================================================
# ChessConnect - Variables d'environnement (PRODUCTION)
# Copier ce fichier vers .env et remplir les valeurs
# =============================================================================

# -----------------------------------------------------------------------------
# Base de données PostgreSQL (OBLIGATOIRE)
# -----------------------------------------------------------------------------
POSTGRES_DB=chessconnect
POSTGRES_USER=chess
# Générer un mot de passe fort: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# -----------------------------------------------------------------------------
# Sécurité JWT (OBLIGATOIRE)
# Générer une clé secrète: openssl rand -base64 64
# -----------------------------------------------------------------------------
JWT_SECRET=CHANGE_ME_USE_64_CHAR_RANDOM_STRING

# -----------------------------------------------------------------------------
# Stripe - Paiements (OBLIGATOIRE)
# Obtenir les clés sur https://dashboard.stripe.com/apikeys
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=sk_live_...
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# -----------------------------------------------------------------------------
# Jitsi Meet - Visioconférence (OBLIGATOIRE)
# Générer un secret: openssl rand -hex 32
# -----------------------------------------------------------------------------
JITSI_APP_ID=mychess
JITSI_APP_SECRET=CHANGE_ME_USE_64_CHAR_HEX_STRING
JITSI_DOMAIN=meet.mychess.fr

# -----------------------------------------------------------------------------
# URL Frontend (OBLIGATOIRE en production)
# -----------------------------------------------------------------------------
FRONTEND_URL=https://mychess.fr

# -----------------------------------------------------------------------------
# Email - Configuration SMTP (Optionnel)
# -----------------------------------------------------------------------------
MAIL_ENABLED=false
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=

# -----------------------------------------------------------------------------
# Administration
# -----------------------------------------------------------------------------
ADMIN_EMAIL=support@mychess.fr

# -----------------------------------------------------------------------------
# Bunny Stream - Hébergement vidéo avec transcoding (Optionnel)
# Pour stocker les enregistrements de cours avec HLS streaming
# -----------------------------------------------------------------------------
BUNNY_STREAM_API_KEY=
BUNNY_STREAM_LIBRARY_ID=590076
BUNNY_STREAM_CDN_HOSTNAME=vz-34fe20be-093.b-cdn.net

# -----------------------------------------------------------------------------
# Bunny Storage CDN - Hébergement fichiers direct (Recommandé)
# Plus simple et moins coûteux que Bunny Stream pour les vidéos MP4
# Créer une zone de stockage sur https://bunny.net/dashboard/storage
# -----------------------------------------------------------------------------
BUNNY_STORAGE_ZONE=mychess
BUNNY_STORAGE_API_KEY=
BUNNY_STORAGE_HOST=storage.bunnycdn.com
BUNNY_CDN_URL=https://mychess.b-cdn.net

# -----------------------------------------------------------------------------
# Web Push Notifications (Optionnel)
# Les clés VAPID sont générées automatiquement au démarrage si non définies
# Pour la production, générez des clés persistantes et définissez-les ici
# -----------------------------------------------------------------------------
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:contact@mychess.fr

# -----------------------------------------------------------------------------
# Backup - Rétention (Optionnel)
# -----------------------------------------------------------------------------
BACKUP_RETENTION_DAYS=30
